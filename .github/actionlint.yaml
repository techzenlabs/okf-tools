# No self-hosted labels remain: every job here names a default hosted runner.
# The file itself stays because checks.workflow hands it to actionlint by
# path, and a named config has to exist.
self-hosted-runner:
  labels: []
