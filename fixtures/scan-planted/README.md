# The negative control

`okf-scan` is believed only after it has been watched failing. The file beside
this one carries a planted private-key header; the gate asserts a non-zero exit
against it before it accepts a clean exit anywhere else.

The planted material is synthetic. The body is not a key and never was.

This directory is excluded from the repository's own self-scan, and the
exclusion is printed by every run that uses it, so an exemption is never
silent.
