---
type: "Reference"
title: "Guides"
description: "A page beside a directory that carries no listing, which loses nothing."
---

# Guides

Measured on Hugo 0.165: this page publishes at `guides/` and every file under
`guides/` still publishes beneath it. Nothing is dropped, so nothing is
reported.
