---
type: "Decision Record"
title: "A decision from elsewhere"
description: "Same type, different bundle, one group."
team: "data-team"
project: "fixture-project"
---

# A decision from elsewhere

Grouping by `type` needs no taxonomy and no second front-matter key, because
Hugo's reserved `type` field is OKF's `type`. This page and the alpha bundle's
decision have to land in one group.
