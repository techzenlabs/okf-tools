---
type: "Guide"
title: "Field guide"
---

# Field guide

Run [`deploy.sh`](../scripts/deploy.sh) against the manifest validated by
[`thing.schema`](../schemas/thing.json). The machine-readable summary sits
beside this page in [`data.json`](data.json).

The whole directory is [`scripts/`](../scripts/), the retired helper was
[`missing.sh`](../missing.sh), the key is [`secret.pem`](../scripts/secret.pem)
and the neighbouring checkout keeps [`outside.sh`](../../outside.sh).
