# Journal

A directory holding a `log.md`. okf-index leaves `log.md` out of every
listing it writes and Hugo publishes it anyway, so the href set inside the
markers can never equal the set of pages Hugo knows about here. The block
below must therefore render exactly as written, markers included.

<!-- BEGIN OKF INDEX -->
* [Entry one](entry-one.md) - The only entry okf-index would list here.
<!-- END OKF INDEX -->
