---
title: "Untyped on purpose"
description: "OKF §11 forbids rejecting this, so the site has to render it."
---

# Untyped on purpose

No `type`. The repository gate would report it; the site renders it, lists it,
and records `"type": null` in the JSON index, because a site that gated on
document quality would let one repository's lint failure take four tenants'
documentation down.
